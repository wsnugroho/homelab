apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: decypharr
  annotations:
    tailscale.com/proxy-group: homelab-ingress
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - decypharr
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: decypharr
                port:
                  name: http
