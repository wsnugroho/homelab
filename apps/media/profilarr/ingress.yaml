apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: profilarr
  annotations:
    tailscale.com/proxy-group: homelab-ingress
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - profilarr
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: profilarr
                port:
                  name: http
