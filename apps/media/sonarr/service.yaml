apiVersion: v1
kind: Service
metadata:
  name: sonarr
  annotations:
    tailscale.com/hostname: sonarr
spec:
  selector:
    app.kubernetes.io/name: sonarr
  ports:
    - name: http
      port: 8989
      targetPort: http
