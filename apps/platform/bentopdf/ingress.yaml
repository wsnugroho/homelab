apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bentopdf
  annotations:
    tailscale.com/proxy-group: homelab-ingress
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - bentopdf
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bentopdf
                port:
                  name: http
