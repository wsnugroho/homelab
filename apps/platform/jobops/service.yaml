apiVersion: v1
kind: Service
metadata:
  name: jobops
  annotations:
    tailscale.com/hostname: jobops
spec:
  selector:
    app.kubernetes.io/name: jobops
  ports:
    - name: http
      port: 3001
      targetPort: http
