apiVersion: v1
kind: Service
metadata:
  name: homepage
  annotations:
    tailscale.com/hostname: homepage
spec:
  selector:
    app.kubernetes.io/name: homepage
  ports:
    - name: http
      port: 3000
      targetPort: http
